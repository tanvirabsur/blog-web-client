import React from 'react';
import BlogCard from '../components/BlogCard';

const Home = () => {
    return (
        <main className="flex-1">
            <BlogCard/>
        </main>
    );
};

export default Home;